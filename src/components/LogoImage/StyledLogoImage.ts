import styled from 'styled-components';
import logo from '../../assets/logo/Logo.svg';
export const StyledLogoImage = styled.div`
    background: no-repeat top left url(${logo});
    overflow: hidden;
    width: 70px;
    height: 70px;
`;