import styled from "styled-components";

export const StyledFavorites = styled.div`
    display: block;

    position: relative;
    max-width: 24px;
    margin: auto auto;
    svg{
        display: block;
        width: 100%;
    }
`;