import styled from "styled-components";
import burger from '../../assets/icon/burger.svg';

export const StyledBurger = styled.div`
    display: block;
    max-width: 732px;
    background-position: 50% 0;
    background: no-repeat url(${burger});
    overflow: hidden;
`;