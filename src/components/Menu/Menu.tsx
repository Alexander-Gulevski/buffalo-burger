import NavItem from "../NavItem/NavItem";
import { StyledMenu } from "./styled";

const Menu = () => {
    return (
        <StyledMenu>
          <NavItem/>
          <NavItem/>
          <NavItem/>
        </StyledMenu>
    );
  };
  
export default Menu;