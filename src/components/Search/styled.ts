import styled from 'styled-components';
import loop from '../../assets/icon/loop.svg';

export const StyledSearch = styled.div`

display: block;
width: 100%;

background: no-repeat url(${loop});
overflow: hidden;

`;